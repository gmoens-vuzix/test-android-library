install:
# -Dversion=1.0 is apparently just a formality, when the actual pom file is created, the release version will be substituted
mvn install:install-file -Dfile=test-android-library.aar -Dsources=test-android-library-sources.jar -Djavadoc=test-android-library-javadoc.jar -DgroupId=com.github.gmoens-vuzix -DartifactId=test-android-library -Dversion=1.0 -Dpackaging=aar -DgeneratePom=true
